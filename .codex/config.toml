# .codex/config.toml

# Conservative defaults. Adjust only after the manual workflow is stable.
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
approval_policy = "on-request"

[agents]
max_threads = 5
max_depth = 1
job_max_runtime_seconds = 1800

[features]
codex_hooks = false
